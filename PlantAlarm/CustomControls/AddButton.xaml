<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:skia="clr-namespace:SkiaSharp.Views.Forms;assembly=SkiaSharp.Views.Forms"
             x:Class="PlantAlarm.CustomControls.AddButton"
             xmlns:tt="clr-namespace:TouchTracking.Forms;assembly=TouchTracking.Forms">
    <Grid>
        <skia:SKCanvasView EnableTouchEvents="true" x:Name="ContainerCanvas" WidthRequest="50" HeightRequest="50"
                           PaintSurface="ContainerCanvasRendered" />
        <Grid.Effects>
            <tt:TouchEffect TouchAction="ButtonTapped" Capture="true" />
        </Grid.Effects>
    </Grid>
</ContentView>