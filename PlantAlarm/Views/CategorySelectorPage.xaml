<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="PlantAlarm.Views.CategorySelectorPage"
             xmlns:controls="clr-namespace:PlantAlarm.CustomControls"
             Title="Select categories"
             Appearing="Handle_Appearing">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Done" Command="{Binding AddCategoriesCommand}" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Margin="0,80,0,0">
            <SearchBar Placeholder="Search for categories..." SearchCommand="{Binding SearchCategoriesCommand}" x:Name="SearchBar"
                       SearchCommandParameter="{Binding Text, Source={x:Reference SearchBar}}" />
            <Grid>
                <CollectionView ItemsSource="{Binding Categories}" ItemSizingStrategy="MeasureFirstItem">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout>
                                <StackLayout Orientation="Horizontal">
                                    <Label Text="{Binding PlantCategory.Name}" VerticalOptions="Center" />
                                    <BoxView HeightRequest="1" HorizontalOptions="Fill" BackgroundColor="Silver" />
                                </StackLayout>
                            </StackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
                <controls:AddButton HorizontalOptions="End" VerticalOptions="End" Margin="0,0,20,60" 
                                    TouchedCommand="{Binding AddCategoryCommand}"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
