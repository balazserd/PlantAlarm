<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlantAlarm.Views.NewPlantPage"
             xmlns:customControls="clr-namespace:PlantAlarm.CustomControls"
             xmlns:customEffects="clr-namespace:PlantAlarm.Effects">
    <ContentPage.Content>
        <ScrollView>
            <TableView Intent="Form" HasUnevenRows="true">
                <TableSection>
                    <ViewCell Height="44">
                        <Grid Margin="15,0,15,0" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="8*" />
                            </Grid.ColumnDefinitions>
                            <Label Text="Name" FontSize="18" VerticalOptions="Center"/>
                            <Entry HorizontalOptions="Fill" Grid.Column="1" HorizontalTextAlignment="End"
                                   Text="{Binding PlantToAdd.Name, Mode=TwoWay}" FontAttributes="Italic">
                                <Entry.Effects>
                                    <customEffects:BorderlessEffect />
                                </Entry.Effects>
                            </Entry>
                        </Grid>
                    </ViewCell>
                    <ViewCell Height="44" Tapped="ShowAddCategoryPage">
                        <Grid Margin="15,0,15,0" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="8*" />
                            </Grid.ColumnDefinitions>
                            <Label Text="Categories" VerticalOptions="Center" FontSize="18" Grid.Column="0"/>
                            <Label Text="{Binding SelectedCategoriesMessage}" VerticalOptions="Center" 
                                   Grid.Column="1" LineBreakMode="WordWrap" MaxLines="2" FontAttributes="Italic"
                                   HorizontalOptions="End"/>
                        </Grid>
                    </ViewCell>
                </TableSection>
                <TableSection Title="Photos">
                    <ViewCell>
                        <StackLayout>
                            <Button Text="Add Photo" HorizontalOptions="Fill" FontSize="17"
                                    Command="{Binding AddPhotoCommand}"/>
                        </StackLayout>
                    </ViewCell>
                    <ViewCell x:Name="PhotosCell">
                        <CollectionView ItemsSource="{Binding Photos}" Margin="3" x:Name="ImageCollectionView"
                                        SizeChanged="SetCollectionViewHeight" HorizontalOptions="FillAndExpand">
                            <CollectionView.ItemsLayout>
                                <GridItemsLayout Orientation="Vertical" Span="3" VerticalItemSpacing="3"
                                                 HorizontalItemSpacing="3"/>
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <customControls:SquaredImageContainerStack>
                                        <customControls:SquaredImage Source="{Binding Photo.Url}" HorizontalOptions="Fill"
                                                                     Aspect="AspectFill" VerticalOptions="Fill">
                                            <customControls:SquaredImage.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding ShowOptions}" />
                                            </customControls:SquaredImage.GestureRecognizers>
                                        </customControls:SquaredImage>
                                    </customControls:SquaredImageContainerStack>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </ViewCell>
                </TableSection>
            </TableView>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
