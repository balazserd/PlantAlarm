<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlantAlarm.Views.NewPlantPage"
             xmlns:customControls="clr-namespace:PlantAlarm.CustomControls">
    <ContentPage.Content>
        <ScrollView>
            <TableView Intent="Form" HasUnevenRows="true">
                <TableSection>
                    <EntryCell Height="44" Label="Name" />
                    <ViewCell Height="44" Tapped="ShowAddCategoryPage">
                        <StackLayout Orientation="Horizontal" Margin="14,0,10,0">
                            <Label Text="Category" VerticalOptions="Center" FontSize="17"/>
                            <Label Text="{Binding SelectedCategoriesMessage}" HorizontalOptions="EndAndExpand" 
                                   VerticalOptions="Center"/>
                        </StackLayout>
                    </ViewCell>
                </TableSection>
                <TableSection Title="Photos" >
                    <ViewCell>
                        <StackLayout>
                            <Button Text="Add Photo" HorizontalOptions="Fill" FontSize="17"
                                    Command="{Binding AddPhotoCommand}"/>
                        </StackLayout>
                    </ViewCell>
                    <ViewCell x:Name="PhotosCell">
                        <CollectionView ItemsSource="{Binding Photos}" Margin="3" x:Name="ImageCollectionView"
                                        SizeChanged="SetCollectionViewHeight" HorizontalOptions="FillAndExpand">
                            <CollectionView.ItemsLayout>
                                <GridItemsLayout Orientation="Vertical" Span="3" VerticalItemSpacing="3"
                                                 HorizontalItemSpacing="3"/>
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <customControls:SquaredImageContainerStack>
                                        <customControls:SquaredImage Source="{Binding Url}" HorizontalOptions="Fill"
                                                                     Aspect="AspectFill" VerticalOptions="Fill" />
                                    </customControls:SquaredImageContainerStack>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </ViewCell>
                </TableSection>
            </TableView>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
